import React from 'react';

const Player = props => {
    return (
        <div>
            <h1>{props.name}</h1>
            <h2>{props.team}</h2>
            {/* Contract Details */}
            {/* Scoring */}
        </div>
    );
};

export default Player;